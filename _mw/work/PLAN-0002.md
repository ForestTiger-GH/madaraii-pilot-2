# PLAN-CBR-002 — Post-Jester Product hardening

**Work kind:** `BOUNDED_WORK_PLAN_FORMATION` (`MADARAII-30`)  
**Work:** `WORK-CBR-002`  
**Baseline:** current `PRODUCT-CBR-001@4ee8583b3feff7775a956c51812232fa0d0516d2`  
**Delta:** `RES-DELTA-CBR-002`  
**Plan status:** admitted for execution under the 2026-09-15 Commission

## Outcome

Produce one exact Product candidate that closes the justified Jester-derived correctness/reliance deltas without widening the source universe or product purpose.

## Execution slices

| Slice | Product work | Blocking checks |
|---|---|---|
| PJ1 semantic robustness | context inference; note/methodology rejection; durable concept/measure identity; accounting numeric forms | focused synthetic regressions + existing semantic suite |
| PJ2 validation coherence | cross-owner source/revision checks; canonical dimension key; calendar/domain/cardinality gates | Jester-derived negative bundles must fail; ordinary bundles pass |
| PJ3 consumer/query | bilingual source/dimension surfaces and filters; safe pivot; CLI multi-value dims; lineage transparency; reliance gate | persisted synthetic DB tests + CLI parity |
| PJ4 build/source trust | single SourceSpec authority; implementation/runtime fingerprint; OOXML structure and CBR-host checks | fingerprint/source-trust tests; internet/local identity in same runtime |
| PJ5 persistence | atomic direct SQLite replacement; preserve existing build staging semantics | failing replacement keeps previous DB |
| PJ6 publication applicability | audit current 41 workbooks for sheet/row/column visibility, comments and number-format-only semantic cues | machine-readable full-source report; route only evidenced classes |
| PJ7 fan-in | update HOW/docs where behavior changes; exact candidate commit | unit/regression suite green |
| PJ8 independent verification | full live 41-source build + local replay + independent preservation + CSV/SQLite + consumer checks | `MADARAII-33` typed verdict |
| PJ9 integration/closure | admit verified candidate, reconcile owners/front door, audit new contour | `MADARAII-34` then `36`; historical closure preserved |

## Change boundaries

Mutable: `src/cbr_unified/`, tests, verification tooling/workflow, README/docs, current target-HOW reconciliation, this contour's `_mw` state/results/evidence and `WORKSPACE.md` after admission.

Fixed: registry source set/URLs unless source-trust verification proves a current baseline error; `TW-CBR-1` purpose; lossless raw authority; no fuzzy cross-source merge; no implicit aggregation; exact prior Jester/history artifacts.

## Readiness

The plan is executable from current evidence. Jester reproducers provide concrete negative cases; current Product tests provide regression anchors; the existing internet-enabled product verification workflow supplies the required full-source environment. Publication metadata is explicitly evidence-first, so uncertainty there does not authorize speculative semantic behavior.

## Stop/replan conditions

Replan if a proposed correction breaks current 41-source semantics, requires changing `TW-CBR-1`, discovers a new source distinction the current observation model cannot represent, or full-source evidence contradicts the assumed applicability of a Jester-derived repair.